{
  "scripts": {
	"buildServer": "npm run build --prefix _app/server",
	"buildClient": "npm run build --prefix _app/client",
	"devServer": "npm run dev --prefix _app/server",
	"devClient": "npm run dev --prefix _app/client",
	"dev": "npm run devServer & npm run devClient",
	"start": "npm run start --prefix _app/server"
  },
  "dependencies": {
    "@google-cloud/local-auth": "^3.0.1",
    "googleapis": "^144.0.0",
    "node-schedule": "^2.1.1",
    "nodemailer": "^6.10.0"
  }
}
