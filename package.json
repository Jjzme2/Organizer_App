{
  "scripts": {
    "buildServer": "cd _app/server && npm run build",
    "buildClient": "cd _app/client && npm run build",
    "build": "npm run buildClient",
    "devServer": "cd _app/server && npm run dev",
    "devClient": "cd _app/client && npm run dev",
    "dev": "concurrently \"npm run devServer\" \"npm run devClient\"",
    "start": "cd _app/server && node index.js",
    "heroku-postbuild": "cd _app/client && npm install && npm run build && cd ../server && npm install",
    "heroku:log": "heroku logs --tail --app ilytat-organizer"
  },
  "dependencies": {
    "@google-cloud/local-auth": "^3.0.1",
    "googleapis": "^144.0.0",
    "node-schedule": "^2.1.1",
    "nodemailer": "^6.10.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
